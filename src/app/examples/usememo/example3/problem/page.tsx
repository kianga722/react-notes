import React from 'react';

import App from './components/App';

export const metadata = {
  title: "useMemo Example 3 - Without useMemo",
  description: "Example 3 of webapp with issues that can be solved with React useMemo",
};

function useMemoExample3Problem() {
  return(
    <App />
  )
}

export default useMemoExample3Problem;